# Vínculo com a proposta de design

O design do chat agent vive em [`sugestoes/chat-ia-redesign/`](../../../sugestoes/chat-ia-redesign/) (protótipo interativo + documentação), não aqui. Esta pasta só registra o que já foi validado e está pronto para virar código de produção em `v10/chat-agent/`.

## Status

- [ ] Painel lateral docado (substitui `.ia-overlay` central) — proposto, aguardando aprovação
- [ ] Minimizar / restaurar (pílula flutuante) — proposto, aguardando aprovação
- [ ] Redimensionar largura (arrastar borda) — proposto, aguardando aprovação
- [ ] Voz: ditado (STT, Web Speech API) — proposto, aguardando aprovação
- [ ] Voz: leitura (TTS) — proposto, aguardando aprovação
- [ ] Aba "Perfil do Agente" — proposto; perfis já modelados em [`../profiles/agent-profiles.json`](../profiles/agent-profiles.json)

Quando um item for aprovado, mova-o daqui para o roteiro de extração no `README.md` da pasta pai como próximo passo concreto.
